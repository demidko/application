class Story {
  constructor(id, title, url, author, comments, points, time) {
    this.id = id;
    this.title = title;
    this.url = url;
    this.author = author;
    this.comments = comments;
    this.points = points;
    this.time = time;
  }
}